import 'package:flutter/material.dart';

class TimeSheet {
  final TimeOfDay _firstIn, _firstOut, _lastIn, _lastOut;
  // final List<TimeOfDay> _times;

  TimeSheet(this._firstIn, this._firstOut, this._lastIn, this._lastOut);
  // TimeSheet(this._times)
}